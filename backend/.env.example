# Backend environment variables
# Copy this to .env and override as needed

API_HOST=0.0.0.0
API_PORT=8000
ALLOWED_ORIGINS=["http://localhost:3000","http://frontend:3000"]
BTC_TICKER=BTC-USD
DEFAULT_STOCK_TICKER=AAPL
HISTORY_PERIOD=30d
HISTORY_INTERVAL=1d

# Phase 2 — AI Commentary (leave blank to use rule engine fallback)
OPENAI_API_KEY=
